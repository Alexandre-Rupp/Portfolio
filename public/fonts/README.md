Place your Heavy Heap font files here to enable the custom display font in the UI.

Expected filenames (at least one is enough):
- `HeavyHeap.woff2` (recommended)
- `HeavyHeap.woff`
- `HeavyHeap.ttf`

The site already references these files in `src/app/globals.css`.
